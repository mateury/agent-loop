services:
  agent:
    build: .
    container_name: agent-loop
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    # Uncomment to use host Claude CLI auth
    # - ~/.claude:/root/.claude:ro
